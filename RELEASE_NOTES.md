# 1.0.34

- Complete the public SDK branding cleanup: headers, modules, documentation, resources and diagnostics.
- Keep the high-level BharatMapView map and navigation API unchanged.
- Rename vendor-branded low-level types and selectors; rebuild integrations that use those APIs directly.
- Correct per-slice platform and version metadata and strip non-exported symbols without changing the public symbol set.

Source revision: `3f324f2` in the SDK source repository.

Required third-party licenses and external data formats are preserved.
