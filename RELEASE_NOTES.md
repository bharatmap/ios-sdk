# 1.0.39

- Fix `setUPinLayerEnabled(_:)` crashing from recursive setter invocation.
- Preserve explicit U-Pin visibility set before style readiness and reapply it after style reloads.
- Keep `poi`, `house_name`, and `house_number` visibility opposite to the U-Pin layer.

Source revision: `d58ddda` in the SDK source repository.

Required third-party licenses and external data formats are preserved.
