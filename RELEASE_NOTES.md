# 1.0.38

- Fix center-anchored double-tap and two-finger-tap zoom changing the geographic viewport center.
- Apply center anchoring consistently to pinch, quick zoom, double-tap zoom in, two-finger tap zoom out and rotate gestures.
- Respect asymmetric map content insets when calculating the visible viewport center.
- Preserve the existing gesture-anchor behavior when `anchorRotateOrZoomGesturesToCenterCoordinate` is disabled.

Source revision: `b0e7499` in the SDK source repository.

Required third-party licenses and external data formats are preserved.
